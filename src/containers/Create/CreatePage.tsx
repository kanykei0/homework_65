import React from "react";
import AdminPage from "../Admin/AdminPage";

const CreatePage: React.FC = () => {
  return (
    <>
      <AdminPage isEdit={false} />
    </>
  );
};

export default CreatePage;
